#version 450 core

in vec4 fragCol;
in float fragMet;
in vec3 fragPos;
in vec3 fragNor;
in vec2 fragUV;

out vec4 col;

uniform sampler2D tex;

void main(){

    col = fragCol * texture(tex, fragUV) * dot(vec3(1/1.7,1/1.7,1/1.7), fragNor);

}